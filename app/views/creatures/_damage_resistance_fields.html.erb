<li class="control-group nested-fields">
  <div class="controls">
    <%= f.collection_select :location_id, Location.order_by_name, :id, :name, include_blank: true %>
  </div>

  <div class="controls">
    <%= f.number_field :dr %>
  </div>

  <div class="controls">
    <%= f.text_area :notes %>
  </div>

  <div class="controls">
    <%= link_to_remove_association "remove", f %>
  </div>
</li>
