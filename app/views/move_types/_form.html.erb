<%= form_for(move_type) do |f| %>
  <% if move_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(move_type.errors.count, "error") %> prohibited this move type from being saved:</h2>

      <ul>
      <% move_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :size => 80 %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
