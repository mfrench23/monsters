<% all = list.count %>
<% list.includes(:master_trait).order("master_traits.name").each_with_index do |trait, i| -%>
<%= render "show_trait", trait: trait, include_details: true %><%= (i < (all-1) ? "; " : "") -%>
<% end -%>
