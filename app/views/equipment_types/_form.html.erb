<%= form_for(equipment_type) do |f| %>
  <% if equipment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_type.errors.count, "error") %> prohibited this equipment_type from being saved:</h2>

      <ul>
      <% equipment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :base_weight %><br>
    <%= f.text_field :base_weight %>
  </div>
  <div class="field">
    <%= f.label :base_cost %><br>
    <%= currency_symbol %><%= f.text_field :base_cost %>
  </div>
  <div class="field">
    <%= f.label :equipment_category_id %><br>
    <%= f.collection_select :equipment_category_id, EquipmentCategory.order_by_name, :id, :name, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>

  <%= render 'form_campaign_chooser', f: f, campaigns: campaigns, monster: equipment_type %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
