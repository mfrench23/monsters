<p>
  <strong>Name:</strong>
  <%= master_trait.name %>
</p>

<ul>
<% master_trait.traits.each do |trait| %>
  <li>
    <%= link_to trait.creature.name.to_s, trait.creature, :class => "quiet" %><%= ": " unless (trait.level.blank? && trait.notes.blank?) -%>
    <% unless (trait.level.blank? && trait.notes.blank?) %>
      <%= render "creatures/show_trait", trait: trait -%>
    <% end %>
  </li>
<% end %>
</ul>
