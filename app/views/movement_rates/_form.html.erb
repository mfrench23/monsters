<%= form_for(@movement_rate) do |f| %>
  <% if @movement_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movement_rate.errors.count, "error") %> prohibited this movement_rate from being saved:</h2>

      <ul>
      <% @movement_rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :monster_id %><br>
    <%= f.text_field :monster_id %>
  </div>
  <div class="field">
    <%= f.label :terrain_type_id %><br>
    <%= f.text_field :terrain_type_id %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.number_field :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
