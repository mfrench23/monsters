<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= equipment_modifier_category.name %>
</p>

<p>
  <span class="field-label show">Modifiers:</span>
  <%= render "boxed_list", list: equipment_modifier_category.equipment_modifiers.order_by_name %>
</p>

<div class="field">
  <span class="field-label show">Modifying</span>
  <%= render "boxed_list", list: equipment_modifier_category.equipment_types.order_by_name %>
</div>

<% if equipment_modifier_category.notes? %>
  <p>
    <strong>Notes:</strong>
    <%= equipment_modifier_category.notes %>
  </p>
<% end %>

<%= link_to 'Edit', edit_equipment_modifier_category_path(equipment_modifier_category) %> |
<%= link_to 'Index', equipment_modifier_categories_path %>
