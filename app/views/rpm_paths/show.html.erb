<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= rpm_path.name %>
</p>

<p>
  <strong>Notes:</strong>
  <%= rpm_path.notes %>
</p>

<%= link_to 'Edit', edit_rpm_path_path(rpm_path) %> |
<%= link_to 'Index', rpm_paths_path %>
