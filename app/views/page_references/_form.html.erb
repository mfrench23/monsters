<%= form_for(@page_reference) do |f| %>
  <% if @page_reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_reference.errors.count, "error") %> prohibited this page_reference from being saved:</h2>

      <ul>
      <% @page_reference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.text_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :monster_id %><br>
    <%= f.text_field :monster_id %>
  </div>
  <div class="field">
    <%= f.label :pages %><br>
    <%= f.text_field :pages %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
