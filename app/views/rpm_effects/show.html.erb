<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= rpm_effect.name %>
</p>

<p>
  <strong>Cost:</strong>
  <%= rpm_effect.cost %>
</p>

<p>
  <strong>Notes:</strong>
  <%= rpm_effect.notes %>
</p>

<%= render 'show_sources', m: rpm_effect %>

<%= link_to 'Edit', edit_rpm_effect_path(rpm_effect) %> |
<%= link_to 'Index', rpm_effects_path %>
